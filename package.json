{
  "private": true,
  "scripts": {
    "prebuild": "node prebuild.js",
    "dev": "npm run prebuild && next dev",
    "build": "npm run prebuild && next build",
    "export": "npm run prebuild && next build && next export",
    "start": "npm run prebuild && next start",
    "lint": "next lint"
  },
  "dependencies": {
    "date-fns": "^2.11.1",
    "next": "^12.0.7",
    "react": "17.0.2",
    "react-burger-menu": "^3.0.6",
    "react-dom": "17.0.2",
    "remark": "^14.0.1",
    "remark-html": "^15.0.0"
  },
  "devDependencies": {
    "eslint": "8.5.0",
    "eslint-config-next": "12.0.7"
  }
}
